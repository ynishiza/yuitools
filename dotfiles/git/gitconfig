[core]
	fileMode = false
	editor = vim
[user]
	name = Yui Nishizawa
	email = yui@exaptive.com
[push]
	default = nothing
[alias]
	# colors
	# - yellow: SHA1
	# - blue: branch
	# - red:
	# - green: author
	bv = branch -vv
	ls = log --format=\"%C(yellow) %h %Cred %cr %C(green) %cn %C(blue)%d %Creset %s  \" -10 
	lp = log -p
	lst = log --stat
	#
	# Shortcuts
	#
	st = status
	br = branch
	ci = commit
	co = checkout
	df = diff
	dc = diff --cached
	ai = add -i
	cia = commit --amend
[credential]
	helper = store
[sendemail]
	#smtpencryption = ssl
	smtpserver = secure156.inmotionhosting.com
	smtpuser = yui@exaptive.com
	smtpserverport = 587
[merge]
	tool = vimdiff
	conflictstyle = diff3
[color]
	branch = 1
